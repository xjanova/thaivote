# 🎨 Setup Guide: Logo & Favicon Settings

## ⚠️ สิ่งที่ต้องทำเพื่อให้ฟีเจอร์ทำงาน

### 1. รัน Migration

คุณต้องรัน migration เพื่อเพิ่ม settings ใหม่เข้าไปใน database:

```bash
php artisan migrate
```

**หรือ** ถ้าไม่สามารถรัน artisan ได้ ให้รัน SQL โดยตรง:

```sql
INSERT INTO settings (`key`, `value`, `type`, `group`, created_at, updated_at)
VALUES
    ('site_logo', '', 'string', 'appearance', NOW(), NOW()),
    ('site_favicon', '', 'string', 'appearance', NOW(), NOW())
ON DUPLICATE KEY UPDATE updated_at = NOW();
```

### 2. ตรวจสอบ Storage Symlink

ตรวจสอบว่ามี symlink จาก `public_html/storage` ไปยัง `storage/app/public`:

```bash
php artisan storage:link
```

**หรือ** สร้าง symlink เอง:

```bash
cd public_html
ln -s ../storage/app/public storage
```

### 3. สร้างโฟลเดอร์สำหรับเก็บไฟล์

```bash
mkdir -p storage/app/public/images/settings
chmod -R 775 storage/app/public/images
```

### 4. Clear Cache (ถ้ามี)

```bash
php artisan cache:clear
php artisan config:clear
```

---

## 🎯 วิธีใช้งาน

1. เข้าสู่ระบบ Admin
2. ไปที่เมนู **ตั้งค่า** (Settings)
3. ในส่วน **รูปลักษณ์** จะเห็น:
   - **โลโก้เว็บไซต์** - สำหรับแสดงใน Admin Sidebar
   - **Favicon** - สำหรับแสดงใน browser tab
4. อัปโหลดไฟล์ที่ต้องการ
5. กดปุ่ม **บันทึกการตั้งค่า**

---

## 📋 ข้อมูลเพิ่มเติม

### รองรับไฟล์

**โลโก้:**
- PNG, JPG, JPEG, SVG
- ขนาดสูงสุด: 2MB
- แนะนำ: ภาพสี่เหลี่ยมหรือแนวนอน ความสูงประมาณ 40-60px

**Favicon:**
- PNG, JPG, JPEG, SVG, ICO
- ขนาดสูงสุด: 1MB
- แนะนำ: ภาพสี่เหลี่ยมจัตุรัส 32x32px หรือ 64x64px

### ไฟล์ที่ถูกสร้าง/แก้ไข

1. **Backend:**
   - `app/Http/Controllers/Admin/SettingsController.php` - เพิ่ม file upload handling
   - `database/migrations/2026_01_10_064246_add_logo_and_favicon_settings.php` - Migration
   - `routes/web.php` - เพิ่ม API endpoint

2. **Frontend:**
   - `resources/js/pages/Admin/Settings.vue` - เพิ่มฟอร์มอัปโหลด
   - `resources/js/layouts/AdminLayout.vue` - แสดงโลโก้ dynamic
   - `resources/views/app.blade.php` - แสดง favicon dynamic

---

## 🐛 Troubleshooting

### ปัญหา: หน้าอื่นๆ เป็นสีขาว (White Screen)

**สาเหตุ:** JavaScript error หรือ settings ไม่อยู่ใน database

**แก้ไข:**
1. เปิด Browser Console (F12) ดู error
2. ตรวจสอบว่ารัน migration แล้วหรือยัง
3. ตรวจสอบว่า API endpoint `/admin/settings/api` ตอบกลับได้หรือไม่

### ปัญหา: อัปโหลดรูปแล้วไม่แสดง

**แก้ไข:**
1. ตรวจสอบ storage symlink: `ls -la public_html/storage`
2. ตรวจสอบ permissions: `chmod -R 775 storage`
3. ตรวจสอบว่ารูปถูกบันทึกใน `storage/app/public/images/settings/`

### ปัญหา: ไม่เห็นส่วน "รูปลักษณ์" ในหน้า Settings

**แก้ไข:**
1. Clear browser cache (Ctrl+Shift+R)
2. ตรวจสอบว่า `resources/js/pages/Admin/Settings.vue` ถูก compile แล้ว
3. รัน `npm run build` หรือ `npm run dev`

---

## ✅ Checklist การติดตั้ง

- [ ] รัน migration หรือ SQL script
- [ ] สร้าง storage symlink
- [ ] สร้างโฟลเดอร์ `storage/app/public/images/settings`
- [ ] ตั้งค่า permissions ให้ถูกต้อง
- [ ] Clear cache
- [ ] ทดสอบอัปโหลดไฟล์
- [ ] ตรวจสอบว่าโลโก้แสดงใน Admin Sidebar
- [ ] ตรวจสอบว่า Favicon แสดงใน browser tab

---

**หมายเหตุ:** ถ้ายังมีปัญหา ให้ตรวจสอบ logs ที่ `storage/logs/laravel.log`
