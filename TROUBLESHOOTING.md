# 🔧 คู่มือแก้ปัญหา ThaiVote Admin Backend

## ⚡ แก้ไขด่วน - หน้าขาวใน Admin Backend

### 🔍 ตรวจสอบปัญหา (ทำตามลำดับ)

#### 1. เปิด Browser Console
```
Chrome/Edge: กด F12 หรือ Ctrl+Shift+I
Firefox: กด F12
Safari: กด Cmd+Option+I
```

ดูว่ามี **error สีแดง** หรือไม่ แล้วบอกข้อความ error

---

#### 2. ตรวจสอบว่า Login แล้วหรือยัง

**ข้อมูล Admin:**
```
URL: /login
Email: test@example.com
Password: password
```

**หลังจาก login:**
- Dashboard (/admin) - ควรใช้งานได้
- Settings (/admin/settings) - ควรใช้งานได้

---

#### 3. Clear Cache ทั้งหมด

```bash
# ใน terminal
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

**หลังจาก clear cache:**
- Refresh browser (Ctrl+F5 หรือ Cmd+Shift+R)
- ลองเข้าหน้าที่มีปัญหาอีกครั้ง

---

#### 4. Build Assets ใหม่

```bash
# ถ้ามี error ตอน build
npm install
npm run build
```

---

### 🐛 ปัญหาที่พบบ่อย

#### ปัญหา: ไอคอนไม่แสดง
**สาเหตุ:** Dependencies ไม่ครบ

**วิธีแก้:**
```bash
npm install @heroicons/vue
npm run build
```

---

#### ปัญหา: หน้าขาวทุกหน้า ยกเว้น Dashboard และ Settings
**สาเหตุที่เป็นไปได้:**

1. **JavaScript Error**
   - เปิด Browser Console (F12)
   - ดู error สีแดง
   - แจ้ง error message มา

2. **API Endpoint ไม่ตอบ**
   - เปิด Browser Console (F12)
   - ไปที่แท็บ Network
   - Refresh หน้า
   - ดูว่ามี request ไหน **สีแดง (failed)**
   - คลิกดูและแจ้ง error

3. **Missing Data**
   - ตรวจสอบว่ามีข้อมูลในฐานข้อมูล:
   ```bash
   php artisan tinker --execute="
   echo 'Parties: ' . App\Models\Party::count() . PHP_EOL;
   echo 'Elections: ' . App\Models\Election::count() . PHP_EOL;
   echo 'Candidates: ' . App\Models\Candidate::count() . PHP_EOL;
   "
   ```

---

#### ปัญหา: หน้า News หรือ Sources ว่างเปล่า
**นี่ไม่ใช่ bug** - เพราะยังไม่มีข้อมูล

**วิธีเพิ่มข้อมูล:**
1. เข้า `/admin/sources` → คลิก "เพิ่มแหล่งข่าว"
2. เข้า `/admin/news` → คลิก "เพิ่มข่าว"

---

### 📊 ตรวจสอบสถานะระบบ

```bash
# ตรวจสอบว่าทุกอย่างพร้อมหรือยัง
./deploy.sh status
```

**ผลลัพธ์ที่ควรได้:**
```
✓ PHP version: 8.2+
✓ Composer installed
✓ NPM installed
✓ Database connected
✓ .env configured
✓ App key set
✓ Storage linked
✓ Assets built
```

---

### 🔄 แก้ปัญหาแบบรวดเร็ว

**ถ้าไม่แน่ใจว่าปัญหาอยู่ที่ไหน รันคำสั่งนี้:**

```bash
./deploy.sh repair
```

คำสั่งนี้จะ:
- ✓ ติดตั้ง dependencies ทั้งหมดใหม่
- ✓ Build assets ใหม่
- ✓ Clear cache ทั้งหมด
- ✓ Run migrations
- ✓ สร้าง storage link

---

### 🆘 ยังแก้ไม่ได้?

**ส่งข้อมูลเหล่านี้มา:**

1. **Browser Console Error**
   - เปิด F12 → Console
   - Screenshot error สีแดง

2. **Network Error (ถ้ามี)**
   - เปิด F12 → Network
   - Screenshot request ที่ failed

3. **Laravel Log**
   ```bash
   tail -50 storage/logs/laravel.log
   ```

4. **PHP Version**
   ```bash
   php -v
   ```

5. **Node Version**
   ```bash
   node -v
   npm -v
   ```

---

### ✅ Checklist ก่อนรายงานปัญหา

- [ ] Login เข้าระบบแล้ว (test@example.com / password)
- [ ] Clear cache ทั้งหมดแล้ว (php artisan cache:clear)
- [ ] Build assets ใหม่แล้ว (npm run build)
- [ ] ดู Browser Console แล้ว (F12)
- [ ] ดู Network tab แล้ว (F12 → Network)
- [ ] Refresh browser แบบ hard reload (Ctrl+F5)

---

### 📦 ข้อมูลระบบปัจจุบัน

**สถานะฐานข้อมูล:**
- Parties: 58 พรรค ✓
- Elections: 1 การเลือกตั้ง ✓
- Candidates: 45 ผู้สมัคร ✓
- News: 0 ข่าว (ยังไม่มีข้อมูล)
- Sources: 0 แหล่งข่าว (ยังไม่มีข้อมูล)
- Users: 1 ผู้ใช้ (admin)

**หน้าที่ใช้งานได้:**
- ✓ Dashboard (/admin)
- ✓ Settings (/admin/settings)

**หน้าที่ควรใช้งานได้ (มีข้อมูล):**
- /admin/parties (58 พรรค)
- /admin/elections (1 การเลือกตั้ง)
- /admin/candidates (45 ผู้สมัคร)

**หน้าที่ว่างเปล่า (ไม่มีข้อมูล แต่ไม่ใช่ bug):**
- /admin/news (0 ข่าว)
- /admin/sources (0 แหล่งข่าว)
- /admin/users (1 ผู้ใช้ แต่ควรแสดงได้)

---

## 🎯 วิธีแก้ปัญหาด่วนที่สุด

```bash
# 1. Clear ทุกอย่าง
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear

# 2. Build ใหม่
npm run build

# 3. ทดสอบใน Incognito Mode
# Chrome: Ctrl+Shift+N
# Firefox: Ctrl+Shift+P
# Safari: Cmd+Shift+N
```

**ถ้ายัง** หน้าขาว → เปิด F12 → Console → แจ้ง error message มา
